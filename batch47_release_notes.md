# Batch 47：真实支付回调验签 + 订单对账后台 + 会员生命周期自动化

新增能力：

- 微信支付回调验签方案
- 支付宝回调验签方案
- Stripe webhook 验签方案
- 订单金额校验
- 支付失败 / 重复回调处理
- 会员自动续期 / 到期自动降级
- 管理员对账报表
- 退款 / 取消订阅记录
- 结算账户配置记录

注意：Streamlit Cloud 默认只运行 app.py。真实回调接口 api_server.py 需要单独部署到 Render/Railway/云服务器。
